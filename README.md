# ALP_PROGRAMS
An Assembly Language Program (ALP) is a set of simple instructions written using small words like MOV, ADD, and SUB to do a task. It is easier to understand than machine code. These instructions are changed into machine code so the system can run them. In simple words, ALP tells the system step by step what to do.
